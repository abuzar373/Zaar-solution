# dependencies
/node_modules
/.pnpm-store

# next.js build output
/.next/
/out/

# production / local environment variables
.env
.env.local
.env.development.local
.env.production.local

# uploads folder (don't commit uploaded user files, except .gitkeep)
/uploads/*
!/uploads/.gitkeep

# debug / OS files
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store
*.pem
